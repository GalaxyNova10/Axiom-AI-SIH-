# Axiom AI Frontend Environment Variables
# Copy to .env and adjust as needed

# API Base URL — leave empty in development to use Vite proxy (recommended)
# Set to http://localhost:8000 if not using proxy
VITE_API_BASE_URL=
